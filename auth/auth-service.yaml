apiVersion: v1
kind: Service
metadata:
  name: qmessentials-auth-service
  namespace: qmessentials
spec:
  selector:
    app: qmessentials
    service: auth
  ports:
    - protocol: TCP
      port: 3010  # External port
      targetPort: 3010  # Container port
  type: ClusterIP
